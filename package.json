{
  "name": "pairwise-gsb-a11y-workbench",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Deterministic pair-wise comparison workbench for keyboard and assistive-technology semantics across two local builds.",
  "scripts": {
    "test": "tsc --noEmit && vitest run",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^7.0.6",
    "vitest": "^3.2.4"
  }
}
