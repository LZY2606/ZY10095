/**
 * Re-export of the bundled sample session so tests and the web demo analyze
 * exactly the same recorded inputs.
 */
export {
  sampleRequest,
  remountRequest,
  BASELINE_FINGERPRINT,
  CANDIDATE_FINGERPRINT,
} from '../../src/core/sample-data.js';
